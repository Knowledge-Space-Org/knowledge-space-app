ks-stage-deploy:
  tags:
    - ks-stage
  script:
    - hostname
    - whoami
    - sudo su visakh
    - cd /opt/knowledge-space-new/
    - git checkout master
    - git pull
    - npm run build
    - sudo service application restart
    - sudo service nginx restart
