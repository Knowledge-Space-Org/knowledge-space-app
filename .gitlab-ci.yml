ks-stage-deploy:
  tags:
    - ks-stage
  script:
    - hostname
    - whoami
    - ps -p $$
    - cd /opt/knowledge-space-new/
    - git pull
    - git checkout stage
    - export CI=false
    - npm install
    - npm run build
    - sudo service application restart
    - sudo service nginx restart
