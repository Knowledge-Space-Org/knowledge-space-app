ks-stage-deploy:
  tags:
    - ks-stage
  script:
    - hostname
    - whoami
    - sudo su visakh
    - cd /opt/knowledge-space-new/
    - git checkout master
    - git pull
    - export CI=false
    - npm install
    - npm run build
    - sudo service application restart
    - sudo service nginx restart
