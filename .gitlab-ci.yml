ks-stage-deploy:
  tags:
    - ks-stage
  script:
    - hostname
    - cd /opt/knowledge-space-new/
    - git pull
    - npm run build
    - sudo service application restart
    - sudo service nginx restart
